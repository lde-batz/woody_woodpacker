- parasite en assembleur				clear


- insertion dans le code du parasite


- chiffrement de la section text		done

- check header
	ATTENTION: ET_DYN et ET_REL a verif
	if (hdr->e_type != ET_EXEC && hdr->e_type != ET_DYN && hdr->e_type != ET_REL)



- clear code
	- comment			done
	- parasite			done
	- mummap			done
	- ptr < size_ptr	done
	- clean

parasite injection:
	2 decalages
		- decalage de sizeof(Shdr) au niveau de (.bss + 1) jusqu'a la fin (old_file_len - offset_shdr_.bss+1)
		decalage pour mettre le nouvel header de section
		...->.bss->.comment->...
		...->.bss->sizeof(Shdr)->.comment->...

		- decalage de sizeof(parasite) au niveau (offset_bss + size_bss) jusqu'a la fin (old_file_len - )
		...->offset_bss->...->sizeof(Shdr)->...
		...->offset_bss->...->sizeof(Shdr)->...

	remplissage de zero
		remplissage de la section .bss avec des 0 de (addr + size)bss - (addr + size)data

	new_file_len = old_file_len + sizeof(parasite) + sizeof(Shdr)
